name: Build and Test

on:
  push:
    branches:
      - main
      - "agent/**"
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test-and-package:
    runs-on: ubuntu-24.04

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: gradle

      - name: Install Debian packaging tools
        run: |
          sudo apt-get update
          sudo apt-get install -y fakeroot

      - name: Make Gradle wrapper and packaging script executable
        run: chmod +x gradlew packaging/linux/fix-deb-dependencies.sh

      - name: Run verification checks
        run: ./gradlew --no-daemon clean check

      - name: Build application distribution
        run: ./gradlew --no-daemon installDist

      - name: Build Debian package
        run: ./gradlew --no-daemon packageDeb

      - name: Upload test report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pinnacle-text-editor-test-report
          path: build/reports/tests/test
          if-no-files-found: ignore

      - name: Upload Debian package
        uses: actions/upload-artifact@v4
        with:
          name: pinnacle-text-editor-deb
          path: build/jpackage/dist/*.deb
          if-no-files-found: error
